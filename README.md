# PDF Audiobook Reader 🎧

A simple Python application that converts PDF text into spoken audio. It uses a graphical interface to let users select files and features a basic control window.

---

## 🚀 Features

* **File Selection:** Uses a native file explorer to pick any `.pdf` file.
* **Text Extraction:** Reads text content from every page of the selected PDF.
